# OpenTelemetry Instrumentation + Collector Pipeline

This project demonstrates a full observability pipeline using:

- Python FastAPI service
- OpenTelemetry SDK + auto-instrumentation
- OTel Collector
- Jaeger (traces)
- Prometheus (metrics)

## 🚀 How to Run

```bash
docker-compose up --build
